
c_01:
	.01. есть идея написать просмотрщик (treeViwer) для форматов файлов которые у меня сейчас сложились для дерева.

	.02. с отдельными экранами для:
		.заметок.
		.цепочек.
		.концепции (возможно разбив её на секции, что-бы просмотривать проще было).
			.т.е. с под экранами для: факторов, целей, целевых функций управления.
			.пока других вещей сложно назвать - кроме что: вектора текущего состояния, стереотипов.
			.может срезов векторов текущего состояния.... - хотя в первую итерацию кажеться хватит факторов и целей.

		.вольных текстов в рамках структуры одного описания.
		.экрана оглавления.

	.03. при этом из функций:
		.навигация к определённым заметкам/элементам.
		.срезы в экранах - по номерам, секциями (в заметках), диапазонами номеров

	.04. уточню экраны:
		.заметки, цепочки, концепции - к ним ещё срезы - т.е. 6-ть.
		.концепция по секциям: факторы, цели, функции - может и у них срезы - может ещё 3+3 - в зависимости от реализации.

		.оглавление, список текстов, тексты - т.е. 3.


с_02:
	.05. в некоторых случаях хочеться сделать скрытые комментарии (типа сносок), либо ссылки вставить

	.06. разсмотреть воможность приенения Markdown разметки, либо подобного?

	.07. а ведь это идея - из прямо такого как тут текста, с добавлением в него спец символов, делать json.

	.08. к элементам - временные метки - хотя это похоже на скрытый комментарий (всплывающий текст).


с_03:
	.09. оглавление вопросов - либо вид, когда компактно видны только вопросы и можно разворачивать ответы интерисующие.

	.10. навигация по секциям:
		.в смысле набора ссылок, которые проматывали-бы к выбранной секции.
		.в смысле команды перехода к такой-то заметке (по номеру).

	.11. может к некоторым секциям добавлять название - видимое в оглавлении секций, навигации.

	.12. кажеться стоит задуматься о многоуровневом нумерованном списке, потенциально с адресацией к его пунктам.


c_04:
    .13. начал макетировать concept_viewer.

    .14. налетел на задачу определение структуры файлов прикладной концепции.

    .15. и корневого файла concepts/contents.json.

    .16. в корневом файле хотелось-бы помимо описания доступных элементов описывать граф взаимосвязей выделенных
    прикладных концепций - его показывать в виде оглавления.

    .17. оглавление можно показывать по разному:
        .в виде списка всего.
        .в виде графа-рисунка - с разной степенью приближения отображая разные связи и вершины.


с_05:
    .18. для хранения данных выбран json.

    .19. однако:
        .он не удобен для написания.
        .текст самих заметок подразумевает наличие внутри html элементов.

    .20. т.е.:
        .а) чистый текст как этот в такой формат напрямую не переносим - потребуеться преобразование.
        .б) потребуеться редактор для написания, либо расширения в существующие (например в саблайм)

    .21. тут огарчает .п.20.а.

    .22. формат для файла заметок какой?

    .23. 2 простейших варианта:
        .а) [ ["текст заметки 1", "...2", ...], [...], ...] - массив секций в которых массивы заметок секции.
        .б) массив заметок, при этом структура самой заметки - объект с текстом и атрибутами (секция например).

    .24. есть ли в них разница, и что выбрать?

    .25. думал про удобство адресации заметок - и что ему помешать может.

    .26. в размышлениях ранее было про возможность добавлять что-то внутрь (т.е. не только элементы заметка и секция)

    .27. если учесть добавление - то адресация в любом случае разваливаеться, и viewer-у считав файл понадобиться
    внутренняя организация заметок.

    .28. по этому - тут скорее удобство и возможность расширения дальше:
        .по расширению - оба формата вроде равнозначны.
        .по удобству - перенос в виде массива в массиве удобнее, поскольку отражает основную связь проще.

    .29. а при возникновении разширений - ведь они в виде объектов будут а не массивов - вот и ключевая разность.

    .30. вывод - формат из заметки.23.а.


c_06:
    .31. по процессу разработки и разпределению задач.

    .32. изначально я говорил что задам 2 вещи:
        .форматы файлов.
        .макеты экранов - почти только вёрстка, с поправкой что ангулар сразу прикручен и по ней переходить можно.
        + описание идеи редактора словами - что-то на техническое задание похожее.

    .33. в ответ ожидаеться 2 вещи:
        .реализация.
        .и возможные уточнения форматов/идеи/экранов - всплывающие по ходу реализации.

    .34. и сейчас я верно начал с форматов и пожелания какую-либо концепцию отразить в concepts/...

    .35. однако:
        .а) изначально у меня было пожелание отразить процесс и концепцию самого concept-viewer-а;
        .б) но ведь может так статься, что не все элементы в этой концепции потребуються
         и в целях тестирования искать нужные окажеться долго;

        +в) набор элементов (применяемых в тексте заметок и другого) я задать должен, в рамках задания форматов.

    .37 => 35. по хорошему ещё стоит добавить несколько уже описанных используемых файлов, что-бы по ним можно было
    смотреть применяемые элементы.

    .38. вот например как в заметке выше, в некоторых местах я задавал соотношения между заметками,
    кратко подчеркивая их взаимосвязь - если есть уровня 1 к 1-му:
        .=> следует из заметки.
        .<= дополняет заметку.
        .<=> взаимосвязанно.

    .39. на заметки налагаются требования краткости и некой независимости/атамарности.

    .40. подчеркивание взаимосвязей указанных в заметке.38. проистекают из нарушения требований к заметкам
    и явно на это указывают.

    .41. хорошо, когда подобные продолжения находяться в рамках одной страницы, однако возможны варианты когда
    связь идёт с удалёнными друг от друга заметками.

    .42. из этого (заметки 37-41) проистекают:
        .требования-уточнения к форматам.
        .требования к элементу попапу над ссылками на заметки.

    .43. ещё отразить:
        .в concepts/contents.json - в описании элементов возможна карта частей (если заметок много разбивать на файлы);
        .структура папки (прикладной концепции) в concepts/ - наброски и результирующие файлы.
        .зафиксировать набросок идеи ранее оглашённый.
        .из заметок в этой секции выше подметил задачу набросать безсмысленный пример который покроет все элементы.


с_07:
    .44. итак под конец с_06 я налетел на несколько вещей. Заметка.43. отразила только часть из них.

    .45. о концепции подхода к формированию прикладных концепций тоже пару слов нужно сказать.

    .46. так-же о концепции процесса разработки, разграничения обязанностей и неограниченном.

    .47. в основе 3 вещи: налблюдение, диалектика, попытки осмысления/синтеза/формализации.

    .48. из них в основном проистекают "базовые артефакты" - заметки, наброски, цепочки.

    .49. в основном - значи что:
        .взаимосвязь не один к одному.
        .могут сразу проистекать и другие элементы (факторы, цели, способы...).

    .50. набор других элементов сейчас не определён окончательно.

    .51. т.е. к сказанному в секции_01 добавились наброски - имееться в виду наброски текстов либо других элементов.

    .52. а под не "базовыми артефактами" понимаються:
        .структура факторов.
        .вектор целей.
        .описание способов - целевых функций управления.
        .произвольные тексты.
        .собственно концепция - её экран.

    .53. далее отразить:
        .елементы текста: ссылка и цитата (вомзожно срезом).
        .концепция - как сборный текст из структуры факторов, вектора целей, описания способов, и произвольных элементов.
        .цитаты с разным срезанием:
            ..по иерархии (например только верхние уровни в структуре).
            ..по набору - срезы.
        .адресуемые элементы (в частности списки).
        .процесс заполнения, инкрементальность и версионирование.
        .идея цитирования текста набирая его из сегментов, которая позволяет с разрывами цитировать.











